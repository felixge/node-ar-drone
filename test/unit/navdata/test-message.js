var common = require('../../common');
var assert = require('assert');
var test = require('utest');
var createMessage = require(common.lib + '/navdata/message');

// not testing anything yet, Message is just a structure
test('command', {
});
